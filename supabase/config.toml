project_id = "lsisqkrzhtpxyhfmwnhq"

# Only AI functions remaining in runtime are user-triggered:
#   - ai-chat-tutor: manual chat with student (no autostart)
#   - ai-generate-lesson: admin-only, offline content authoring
#   - ai-generate-ort-test: admin-only, offline test authoring
#   - ai-practice-generate: admin-only, offline practice question authoring
#   - ai-classify-review-questions: admin-only, offline quality review
# All analytics / planning / recommendations are now deterministic (no AI runtime).

[functions.ai-generate-lesson]
verify_jwt = false

[functions.ai-chat-tutor]
verify_jwt = false

[functions.ai-generate-ort-test]
verify_jwt = false

[functions.ai-practice-generate]
verify_jwt = false
